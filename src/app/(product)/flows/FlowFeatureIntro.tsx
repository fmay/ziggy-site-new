import { FC } from 'react'
import Intro from '@/components/ui/intro/Intro'

interface FlowFeatureIntroProps {}

const FlowFeatureIntro: FC<FlowFeatureIntroProps> = ({}) => {
  return (
    <Intro title="Flow features" description="Below are a few flow related features." image="" />
  )
}

export default FlowFeatureIntro
